# Implementation Summaries

Permanent implementation records for Constellation belong in this directory.

## Current Status

There are no numbered implementation summaries here yet.

As of 2026-03-14, the tracked plan in `ai_docs/plans/2026-03-13-pluggable-providers/` has its implementation phases and automated checks marked complete, but its review and validation rows are still pending. Per the summarize-plans workflow, do not create a numbered implementation summary until those remaining validation steps are complete.

## Next Record

When the current provider refactor is complete, add the first durable summary here using the next sequential filename:

- `0001-pluggable-providers.md`

## Usage

- Keep permanent implementation writeups here.
- Use `NNNN-` prefixes for chronological ordering.
- Prefer implementation summaries over `ai_docs/plans/` when both exist.
